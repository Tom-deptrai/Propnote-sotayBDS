import 'package:flutter/material.dart';

/// Kích thước canvas bản đồ mock dùng chung giữa Map screen và Location
/// picker, để toạ độ (mapX, mapY) chuẩn hoá 0.0–1.0 nhất quán ở mọi nơi.
const Size mapCanvasSize = Size(1500, 1500);
